[project]
name = "wefinance-recommend"
version = "0.1.4"
description = "Generate explainable investment recommendations grounded in real spending data."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
wefinance-recommend = "wefinance_recommend:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["wefinance_recommend"]
